"use client"; import {useEffect,useState} from "react"; import Link from "next/link"; import api from "@/lib/api"; export default function VerifyEmail(){const[token,setToken]=useState("");const[msg,setMsg]=useState("Reading verification link...");useEffect(()=>{const t=new URLSearchParams(window.location.search).get("token")||"";setToken(t);if(t)api.get(`/auth/verify-email?token=${encodeURIComponent(t)}`).then(r=>setMsg(r.data.message)).catch(e=>setMsg(e?.response?.data?.detail||"Verification failed"));else setMsg("Missing verification token")},[]);return <main className="flex min-h-screen items-center justify-center px-4"><div className="max-w-md rounded-xl border bg-card p-8 text-center"><h1 className="text-xl font-semibold">Email verification</h1><p className="mt-3 text-muted-foreground">{msg}</p><Link className="mt-5 inline-block text-primary hover:underline" href="/login">Continue to sign in</Link></div></main>}
