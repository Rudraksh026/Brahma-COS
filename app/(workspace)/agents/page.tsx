"use client"; import {useEffect,useState} from "react"; import api from "@/lib/api"; import {AgentMonitor} from "@/components/agents/agent-monitor"; import type {Agent} from "@/lib/types"; export default function AgentsPage(){const[agents,setAgents]=useState<Agent[]>([]); useEffect(()=>{api.get("/agents/").then(r=>setAgents(r.data)).catch(()=>setAgents([]))},[]); return <AgentMonitor agents={agents}/>;}