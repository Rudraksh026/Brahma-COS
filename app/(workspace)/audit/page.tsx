"use client"; import {useEffect,useState} from "react"; import api from "@/lib/api"; import {AuditLedger} from "@/components/audit/audit-ledger"; import type {AuditEvent} from "@/lib/types"; export default function AuditPage(){const[events,setEvents]=useState<AuditEvent[]>([]); useEffect(()=>{api.get("/audit/").then(r=>setEvents(r.data)).catch(()=>setEvents([]))},[]); return <AuditLedger events={events}/>;}