"use client"; import {useEffect,useState} from "react"; import api from "@/lib/api"; import {DecisionReview} from "@/components/decisions/decision-review"; import type {Task} from "@/lib/types"; export default function DecisionsPage(){const[tasks,setTasks]=useState<Task[]>([]); const load=()=>api.get("/tasks/").then(r=>setTasks(r.data.filter((t:Task)=>t.status==="BLOCKED"))); useEffect(()=>{load()},[]); return <DecisionReview tasks={tasks} onChanged={load}/>;}