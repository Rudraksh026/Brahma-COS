"use client"; import {useEffect,useState} from "react"; import api from "@/lib/api"; import {MemoryLedger} from "@/components/memory/memory-ledger"; import type {MemoryItem} from "@/lib/types"; export default function MemoryPage(){const[items,setItems]=useState<MemoryItem[]>([]); const load=()=>api.get("/memory/").then(r=>setItems(r.data)); useEffect(()=>{load()},[]); return <MemoryLedger items={items} onChanged={load}/>;}