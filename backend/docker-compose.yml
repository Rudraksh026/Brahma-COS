version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: brahma
      POSTGRES_PASSWORD: brahma_password
      POSTGRES_DB: brahma_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
